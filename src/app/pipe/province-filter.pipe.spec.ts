import { ProvinceFilterPipe } from './province-filter.pipe';

describe('ProvinceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProvinceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
