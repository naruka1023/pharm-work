import { WorkExperiencePipe } from './work-experience.pipe';

describe('WorkExperiencePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkExperiencePipe();
    expect(pipe).toBeTruthy();
  });
});
