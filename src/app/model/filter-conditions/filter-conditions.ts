export interface filterConditions{
    dateFilter?: boolean;
    JobType?: boolean;
    JobTypeTwo?: boolean
    timeFrame?: boolean; 
    location?: boolean;
    filterFlag?: boolean;
    header: string
}