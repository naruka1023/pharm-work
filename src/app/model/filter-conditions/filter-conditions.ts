export interface filterConditions{
    dateFilter?: boolean;
    JobType?: boolean;
    categorySymbol: string;
    JobTypeTwo?: boolean
    timeFrame?: boolean; 
    location?: boolean;
    filterFlag?: boolean;
    header: string
}